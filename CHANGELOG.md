# 更新日志

## 1.10.2

- 修复 Token 统计：补上缓存读写（cache.read/write），使用记录显示「总 Token」与明细（输入/输出/思考/缓存读/缓存写）。
- 新增「关于」区块：插件名称与版本、发布者、OpenCode 后端版本、项目主页链接。
- 新增多国语言（中文 / English），可在设置页切换，自动跟随 VS Code 语言偏好。
- 新增首 token 响应时间：生成期间在活动栏显示「首 token x.xs」。

## 1.10.1

- 上下文占用显示优化：环形旁显示「已用/上限」数字，占用按模型上下文窗口计算，悬浮可看详细数值。
- 修复对话体验：AI 一个回合会产生多条消息，现在连续 AI 消息只显示一个头像、紧凑对齐，不再「每用一次工具就换一个头像」。
- 新增「使用记录」：设置页展示当前会话的消息数、工具调用次数、Token 用量（输入/输出/思考）与成本。

## 1.0.9

- 修复核心流式问题：`/event` 事件流按项目目录隔离，订阅时补上 `directory` 参数，流式更新（文字/思考/工具/权限）全部恢复。
- 思考预览改为滑动窗口，跟随流式展示最新内容。
- 计划模式完成后自动在右侧编辑器打开文件差异预览。

## 1.0.8

- 重写流式核心：正确处理 `message.part.delta` 增量事件（缺失的 part/消息自动创建，不再丢失增量），`message.part.updated` 全量替换，并防重复追加。
- 流式输出体验对标 Claude Code：最后一条生成中消息显示闪烁光标，仅更新受影响的单条消息（memo 化），消除整列重渲染造成的卡顿与闪烁。
- 工具调用块优化：运行时自动展开、显示命令输入与执行计时，完成后自动展示输出，错误状态更清晰。
- 新增顶部粘性活动栏：生成期间始终显示当前状态、正在使用的工具、耗时与思考预览，并提供醒目的「停止」按钮（此前停止按钮仅存在于底部输入区，滚动时不可见）。
- 智能自动滚动：用户上翻查看历史时不再强制滚动到底部，回到底部附近后恢复跟随。

## 1.0.7

- 修复流式输出：处理 message.part.delta 增量事件，文字与思考过程实时流式显示。
- 修复发送消息后历史消息闪烁/消失的问题（移除发送后立即全量刷新导致的竞态）。
- 事件流断开后自动重连，避免生成中途卡住。

## 1.0.6

- 下拉菜单改为视口感知定位（fixed + 边界钳制），窄屏下不再被遮挡；支持点击外部/Esc 关闭。
- 补全所有按钮与选项的悬浮提示。
- 新增动效：下拉菜单、会话抽屉、消息、Toast、权限卡片、按钮过渡。
- 会话管理改为抽屉式侧栏：点击「会话」展开，Esc 或点击空白处收起。
- 新增上下文占用环形指示（token / 上下文窗口上限）。
- 抽屉内新增「设置」页：服务状态、环境配置（命令路径/端口）、模型与代理概览。

## 1.0.5

- 无会话时自动新建会话；有会话则记住上次会话继续。
- 记忆用户偏好（模型 / 智能体 / 审批模式 / 当前会话），重启后恢复。
- 新增「命令」按钮，可浏览并运行 opencode 全部原生命令。
- 等待输出时显示实时计时、token 统计与思考内容预览。
- 输入区工具栏改为可换行布局，适配窄窗口，修复中文对齐错乱。

## 1.0.4

- 修复发送消息 400 错误（消息 part 的 id 前缀不符合 opencode 要求）。
- 新增审批模式（默认询问 / 自动接受编辑 / 全部自动 / 计划模式）。
- 完善按钮标签与悬浮提示，代理更名为智能体。
- 新增斜杠命令菜单、空会话示例提问、断连横幅与重连按钮。

## 1.0.3

- 完善发布配置（图标、许可证版权信息）。

## 1.0.2

- 新增活动栏图标与侧边栏视图入口。

## 1.0.1

- 初始版本。
- 中文图形界面（Webview，非终端）。
- 会话管理：新建/重命名/删除/复制/切换/搜索/分享/摘要。
- 流式对话，Markdown + 代码高亮 + 复制。
- 思考过程、工具调用、任务列表、权限授权可视化。
- 模型与代理选择、文件上下文、选中代码加入/解释。
- token/成本统计、服务状态栏与设置页。
